// Import components
import Navbar from "../component/navbar";

const Register = () => {
    return (
        <div>
            <Navbar />
            register
        </div>
    );
};

export default Register;